# Install torch FIRST, separately, before this file — see SPEECH_SETUP.md.
# Mixing a CUDA-specific --index-url with a plain requirements.txt install is
# unreliable for a package (torch) that's published under the same name on
# both PyPI (CPU-only) and PyTorch's own index (CUDA) — pip's resolver can
# silently pick either one depending on install order. Installing it as its
# own explicit step avoids that ambiguity entirely.
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pydantic
faster-whisper
kokoro>=0.9.4
soundfile
numpy
# Already installed transitively via faster-whisper (it bundles its own
# FFmpeg libs for audio decoding), but declared explicitly here since /speak
# now also uses it directly to encode Ogg/Opus voice notes for Telegram —
# an undeclared transitive dependency is too fragile to build a feature on.
av